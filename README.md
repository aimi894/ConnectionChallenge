
  # Create clickable app with dropdown

  This is a code bundle for Create clickable app with dropdown. The original project is available at https://www.figma.com/design/gEtkLnzfY1QpUz1ZvwltJS/Create-clickable-app-with-dropdown.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  